# Brian decisions and remaining operational verification

Brian authorized the retained-feature integration. Ang accepted all five exact source candidates; earlier implementation/privacy freezes for the rows below are superseded. This ledger indexes the authoritative behavior and immutable inventory in [FEATURE_DISPOSITION.md](FEATURE_DISPOSITION.md). It does not certify deployment or activation.

| Cluster | Settled decision | Remaining qualification / owner |
|---|---|---|
| FC-11 | Implement the private cron execution/Calendar mirror candidate. | Ops `t_907703d2`: sole-owner Calendar, grants/config and real-result UAT. No live compatibility claim. |
| FC-37 | Keep an OS-owner CLI, NOT a chat slash command: recorded evidence by default and explicitly selected current-memory reconstruction. Cloud/local_external uses either a valid injective `{profile}` bank template or an explicit static `bank_id` / legacy `banks.hermes.bankId` when the template is absent or empty; privacy fails closed. | No live compatibility claim or live memory calls. No import, backfill, retention change or stored-data migration. |
| FC-41 | `/new (<prompt>)` is wanted and implemented: literal first user message in the source profile; retain title/bare reset and confirmation semantics. | Integrated source; parent exact-SHA hosted staging CI and deployment remain separate. Bounded process-local dedupe is not crash-durable exactly-once delivery. |
| FC-42 | Private `/log`, one midnight batch and intentional finalized journal ingestion into the correct profile scope are approved. | CLI/TUI + authorized Telegram/Discord delivered text only; no voice/media/desktop/split continuations. Ops configuration/activation prerequisite `t_907703d2`; raw retention default, destructive duplicate cleanup off. Cleanup interval remains undecided and is not an activation blocker. |
| FC-43 | Historical blanket journal-to-Hindsight block explicitly **SUPERSEDED** by Brian’s intentional finalized journal decision. | No accidental chat ingestion or mass backfill/deletion. Historical inventory retained once; no new global name-based memory gate. |
| FC-44 | New standalone sandbox-qualified controller accepted; old framework not restored. | NOT installed/production-qualified. Parent Ang must establish lifecycle/topology, frozen authority, readiness/health and rollback/recovery prerequisites separately. No fake receipts or production inference from sandbox results. |

Current release owner is `t_ddd2e9dc`. Brian's renewed one-batch authorization is recorded in [NEXT_SLICE.md](NEXT_SLICE.md): start `2026-09-07T05:34:54-07:00`, latest forward cutoff `2026-09-07T09:04:54-07:00`, hard expiry `2026-09-07T09:34:54-07:00`. It closes on success and does not renew or carry forward old multi-batch permission. Brian explicitly deferred Google OAuth and Calendar UAT until `2026-09-08`; they are not source-release blockers. Old incident/reconstruction graphs remain held. Other feature decisions remain unchanged. No further Brian product decision is needed to complete this source integration; operational prerequisites and exact-SHA hosted CI still gate release.
