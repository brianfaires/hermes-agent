# Independent targeted CI-tail review

Decision: **accepted; no blocking findings**. Reviewed only the finite correction delta from `bdc5d6de3541d2deb95bf12198113ecea493c3a0` to `29ffab0af5ae088c0040ffaaa95b68c551b72012`, tree `e407f1261ac2748e5a6fa44158a7d02a850d00b3`. Exact SHA256 and Git blob bindings for all20 changed paths are in `ci-tail-independent-bindings.json`. This is one targeted review; no completed-feature audit or slow release rerun.

The indexed20 failures and raw shard traces agree with the corrections. Nineteen are obsolete fixture expectations/lifecycle context. The exact-pin packaging failure is a real manifest policy omission: only approved davey and PyNaCl receive explicit exceptions in both project and lock; no dependency graph, artifact, global14-day cutoff or scan broadening. Reviewed the actual offline `uv lock --check` return0 receipt and structural lock comparison. No production Python policy was changed.

Literal MEDIA corrections retain a successful standalone file embedding with exact encoded bytes and prove inline/prose examples with existing files remain literal without reads. Discord overflow now reconstructs every escaped payload byte and checks all chunk bounds/counters. Normal generator receipt uses isolated task HOME and current renderers; regenerated vectors retain corpus and expectation categories. Independent regeneration pytest passes. Quoted standalone directives remain positive delivery while prose remains visible.

Notification positives now provide real named owner homes/configs and explicitly allowed platforms under deny mode. They preserve exact delivery/subscription assertions, metadata propagation, foreign subscription exclusion, secondary-only/no-default adapter pressure and lifecycle cursor/archive behavior. Adjacent policy tests retain missing/foreign home refusal, no ambient destination inheritance, claim-to-send denial and cursor rewind. Shutdown fake models the publication helper and additionally checks clear=True while retaining real stop cleanup assertions.

Skill fixtures respect the approved1024 prompt budget and existing hard authoring limit. Exact-limit editing persists all bytes without preview; over-authoring-limit editing fails without mutation; externally authored oversized content still exercises exact truncated preview. The sole renamed hosted case is explicitly mapped in machine bindings; no assertion deletion or skip substitutes for its valid-edit behavior. Linter warning boundary remains asserted on both sides.

Final worker qualification was independently reconciled from selected raw reports: **403 passed across22 complete files,28 subtests passed**, plus one pre-existing optional aiohttp_socks skip in platform-base. All20 indexed case contracts are present in qualified complete files;19 keep their names and the skill-manager case has its explicit replacement plus stronger boundary cases. Red intermediate receipts remain preserved and are superseded only by subsequent complete-file successes. No indexed failed case is skipped.

Independent actual command (one fresh isolated process per file):

```text
python ../run-pytest-focused.py source ci-tail-independent-pytest.json tests/gateway/test_api_server_media_data_urls.py tests/gateway/test_discord_edit_message_overflow.py tests/gateway/test_v021_notification_policy.py tests/gateway/test_v021_notifier_profile_qualification.py tests/gateway/test_shutdown_cache_cleanup.py tests/tools/test_skill_manager_tool.py tests/test_packaging_metadata.py tests/conformance/test_vector_generator.py
```

Runner return0; all eight file returns0, **108 passed,0 failed,0 skipped**. Canonical child argv, actual output and raw receipt hash are embedded in machine bindings. One existing discord audioop deprecation warning is non-failing. Independent checks are a repeated subset, not added to403. `git diff --check bdc5d6de..29ffab` returned0.

Ang's checklist follow-up (five retained manual checks and rollback boundaries) is byte-identical to the base. The entire ledger was byte-identical when reviewed source qualification started; parent's later additive documentation preserves entire rows and historical fields. No feature status or manual check has been completed by this correction. All78 feature IDs and all pending manual checks retain their dispositions. New documentation/final commit is separately bound by parent after this source review.

Reviewer made no source edits, spawned no agents, installed nothing and changed no staging/main/live state. Independent pytest process completed and no reviewer subprocess remains. Acceptance is local qualification only: CI34553356412 failed at the base, and Ang's next exact-candidate hosted CI is still required.
