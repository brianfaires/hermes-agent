# Finite CI tail correction

Base `bdc5d6de3541d2deb95bf12198113ecea493c3a0` retained clean, including Ang's checklist follow-up. Reviewed implementation candidate `29ffab0af5ae088c0040ffaaa95b68c551b72012`, tree `e407f1261ac2748e5a6fa44158a7d02a850d00b3`. Full hosted CI34553356412 exposed20 new cases across6 shards after the previous26 were corrected; this is a bounded correction, not new feature work or a repeated completed-feature audit. No full hosted green is claimed. Ang owns next staging integration/push and exact-candidate full CI. Workers make no staging/main/live writes or pushes.

## Qualification and contracts

**403 passing cases across 22 complete files**, plus28 subtests excluded from case totals. All20 indexed failed cases are qualified, one skill-manager case maps to its current-budget replacement with added hard-limit and preview cases. One existing optional `aiohttp_socks` skip in platform_base remains; no indexed case skipped and no new skips. No slow release fixture, full local suite, previous26 audit or new UAT gate was run. Exact actual pytest argv, returns, outputs, normal generator command and offline resolver command are in V021_APPROVED_CI_TAIL_TESTS.json. Red local intermediate receipts remain with their true returns.

The genuine product metadata defect is limited to absent `davey` and `pynacl` exact-pin exceptions under the existing exclude-newer policy. Manifest and uv.lock add only those two names, each false. The global14-day policy, all other exception values, package graph/artifact hashes and scan policy remain unchanged. Actual `uv lock --check --offline --python /home/brian/.hermes/hermes-agent/.venv/bin/python` returned0 (255 packages); fresh task HOME/cache, no installs or downloads.

The remaining failures were outdated fixture/data expectations: standalone MEDIA directives remain deliverable while literal examples are preserved without file reads; Discord full escaped overflow payload and deliberate code/table structure are asserted. Vectors were regenerated by the normal script using isolated task HOME, never hand-edited. Notification fixtures explicitly grant only the intended platform in a real owner home while retaining foreign/no-default/denial and lifecycle assertions. Shutdown fake now records coverage clear=True and retains agent cleanup assertions. Skill tests cover the approved1024 prompt budget independently from the1024 authoring hard limit; invalid edits retain original bytes and external legacy content still gets an exact bounded preview. Production identity, notification, text and validation logic is unchanged.

All writes stayed in source/task evidence. Canonical Python and pinned task-only pytest dependencies reused read-only through retained runner. No environment/config/venv install, live/model/paid-service use, service restart, voice/retired/archive work, staging/main mutation or push. Local temp configuration is only isolated test fixture state.

## Exact complete-file inventory

- `tests/conformance/test_vector_generator.py`: 4 passed; `ci-tail-text-pytest.json`.
- `tests/gateway/test_stream_consumer.py`: 66 passed; `ci-tail-text-pytest.json`.
- `tests/gateway/test_api_server_media_data_urls.py`: 5 passed; `ci-tail-text-pytest.json`.
- `tests/gateway/test_platform_base.py`: 103 passed; `ci-tail-text-pytest.json`.
- `tests/gateway/test_v021_discord_text.py`: 4 passed; `ci-tail-text-pytest.json`.
- `tests/gateway/test_discord_edit_message_overflow.py`: 8 passed; `ci-tail-text-pytest.json`.
- `tests/gateway/test_v021_text_compatibility.py`: 8 passed; `ci-tail-text-pytest.json`.
- `tests/gateway/test_discord_format.py`: 7 passed; `ci-tail-text-pytest.json`.
- `tests/gateway/test_v021_notifier_profile_qualification.py`: 1 passed; `ci-tail-notify-pytest.json`.
- `tests/gateway/test_kanban_notifier.py`: 12 passed; `ci-tail-notify-pytest.json`.
- `tests/tools/test_kanban_tools.py`: 32 passed; `ci-tail-notify-pytest.json`.
- `tests/hermes_cli/test_kanban_notify.py`: 26 passed; `ci-tail-notify-pytest.json`.
- `tests/gateway/test_v021_notification_policy.py`: 12 passed; `ci-tail-notify-pytest.json`.
- `tests/gateway/test_v021_notifier_diagnostics.py`: 1 passed; `ci-tail-notify-pytest.json`.
- `tests/gateway/test_v021_background_notify_retained.py`: 3 passed; `ci-tail-notify-pytest.json`.
- `tests/test_packaging_metadata.py`: 10 passed; `ci-tail-core-corrected.json`.
- `tests/tools/test_skill_linter.py`: 19 passed; `ci-tail-core-final.json`.
- `tests/tools/test_skill_manager_tool.py`: 65 passed; `ci-tail-skill-manager-final.json`.
- `tests/gateway/test_shutdown_cache_cleanup.py`: 3 passed; `ci-tail-core-final.json`.
- `tests/tools/test_v021_discord_packaging.py`: 1 passed; `ci-tail-core-final.json`.
- `tests/agent/test_v021_prompt_contracts.py`: 3 passed; `ci-tail-core-final.json`.
- `tests/gateway/test_v021_served_coverage.py`: 10 passed; `ci-tail-core-final.json`.

## Cohesive commits

b8310a6c9dd1a659ef5cc2e3b16d89e9ff819cf1 test(text): align literal media and escaped Discord conformance contracts
88fa75f2137c79216a71ec5fc5486a8be31d5af6 test(kanban): qualify notification fixtures with explicit owner policy
29ffab0af5ae088c0040ffaaa95b68c551b72012 fix(packaging): align reviewed pin exceptions and current fixture contracts

## Owner detail and unsuccessful intermediate attempts

---

ci-tail-text-evidence.md

# CI tail text correction

Pre-mutation compatible contracts: approved standalone MEDIA directives deliver images; prose, quoted and code examples remain literal even when the referenced file exists. Stream cleanup and actual extraction must agree. Discord escapes prose markdown while preserving inline/fenced code and renderer-generated table structure. Overflow must preserve the entire escaped payload across bounded chunks. The normal conformance generator remains the source of generated vectors. No product change is indicated by the seven indexed failures; old expectations/data predate the approved literal contracts. All work stays source/task evidence; no installs, live configuration, staging, push or voice changes.

Read exact index ci-bdc5d6d-failures.json and failure traces in slices 1, 2, 3, 6 and 7. Pending actual per-file pytest qualification and normal generator receipt below.

Completed correction commit `b8310a6c9dd1a659ef5cc2e3b16d89e9ff819cf1`, tree `25a79baf31057d4438f8da145c814546ae6d25b9`. Ten owned paths only, no runtime changes.

API standalone positive now asserts exact encoded PNG bytes; code/prose examples with real existing files remain identical and must not call read_bytes. Stream still asserts quoted standalone removal plus exact prose preservation. Base extraction retains actual attachment and exact inline example. Overflow now checks every chunk length/counter and reconstructs the complete escaped payload, strengthening old end-marker-only check. Earlier v021 test preserves table bullets/values with the approved literal emphasis bytes. Existing function names retained so all seven indexed case bindings remain direct.

Actual normal generator command: `/home/brian/.hermes/hermes-agent/.venv/bin/python -B scripts/generate_conformance_vectors.py`, return0,44 vectors each Discord/Slack/Telegram/WhatsApp. Full isolated environment and outputs in ci-tail-vectors-generation.json. HOME/HERMES_HOME/TMPDIR task-local, environment constructed without ambient live settings. Discord expected outputs change; other three generated files change oracle provenance only. Generator description corrected to current literal contract. No hand-edited vector values or scan bypass.

Actual pytest command: `python ../run-pytest-focused.py source ci-tail-text-pytest.json tests/conformance/test_vector_generator.py tests/gateway/test_stream_consumer.py tests/gateway/test_api_server_media_data_urls.py tests/gateway/test_platform_base.py tests/gateway/test_v021_discord_text.py tests/gateway/test_discord_edit_message_overflow.py tests/gateway/test_v021_text_compatibility.py tests/gateway/test_discord_format.py`. Runner return0; every file return0. Raw exact canonical child commands/outputs in ci-tail-text-pytest.json. Counts:4+66+5+103+4+8+8+7=205 passes,11 subtests (not counted as cases). One pre-existing base fixture importorskip for absent aiohttp_socks; no skips added, no failed indexed case skipped. No installs requested/performed.

`git diff --cached --check` return0 before commit. Machine bindings ci-tail-text-bindings.json record exact10path SHA256/gitblob plus raw receipt SHA256. Worker test session terminated naturally return0; no children spawned, no remaining worker subprocess. Independent review belongs to parent after all clusters green.

---

ci-tail-notify-evidence.md

Notification correction pre-mutation contract

The seven indexed notification failures exercise positive delivery/subscription with nonexistent named owners or adapters without their construction-time home. Real policy resolution now requires that owner configuration context, and fails closed when unavailable. Tests will supply disposable existing owner homes with config.yaml explicitly allowing only telegram (or tui for local subscription), mode deny otherwise. Existing exact delivery counts, metadata, lifecycle/cursor/archive behavior remain. Foreign subscription and absent-default adapter negatives remain; adjacent notification-policy suites qualify denial and no ambient/default inheritance. No product policy changes are proposed. Tool worker fixture currently says test-worker in HERMES_PROFILE while its physical home is the default home; its owner context will become an actual profiles/test-worker home.

Owned files: tests/gateway/test_v021_notifier_profile_qualification.py, tests/gateway/test_kanban_notifier.py, tests/tools/test_kanban_tools.py, tests/hermes_cli/test_kanban_notify.py. Raw indexed failures read from ci-bdc5d6d-failures.json and slice2/5/6 traces; zero sends/subscribed=False match missing-owner rejection. All writes remain task/source.

Final qualification

Command: `python ../run-pytest-focused.py source ci-tail-notify-pytest.json tests/gateway/test_v021_notifier_profile_qualification.py tests/gateway/test_kanban_notifier.py tests/tools/test_kanban_tools.py tests/hermes_cli/test_kanban_notify.py tests/gateway/test_v021_notification_policy.py tests/gateway/test_v021_notifier_diagnostics.py tests/gateway/test_v021_background_notify_retained.py` returned0. Counts in order:1,12,32,26,12,1,3 =87 passed; background also12 subtests. No skips/failures. Raw exact pytest commands, source interpreter/dependency isolation and outputs in ci-tail-notify-pytest.json. No production files changed. No removed assertions; policy config uses deny with only intended platform allowlisted and preserve_tui=False. Secondary adapter has actual runtime_profile_home; named standalone fixtures have pinned launch home; worker physical named home matches persisted owner; CLI wake adapter pins owner home. Adjacent real policy suite retains owner missing, foreign unconfigured home, no ambient destination inheritance, malformed policy denial, claim-to-send denial/cursor rewind, redirect and TUI behavior. Prior foreign subscription remains unseen and secondary-only runner still has no default adapter. Full-file tests and runner are complete; no agent children spawned.

Commit 88fa75f2137c79216a71ec5fc5486a8be31d5af6; tree 7e267fb54c7a05f51feff0dda7139129eb4b5716. Owned files exact SHA256/blob bindings in ci-tail-notify-bindings.json. `git diff --cached --check` passed prior commit. Shared source other-agent mutations excluded from this commit.

Binding clarification: notification-owned correction commit is 88fa75f2137c79216a71ec5fc5486a8be31d5af6, tree 7e267fb54c7a05f51feff0dda7139129eb4b5716. The first post-commit binding read observed the concurrent parent's later core commit29ffab0af5ae088c0040ffaaa95b68c551b72012, tree e407f1261ac2748e5a6fa44158a7d02a850d00b3; that is a descendant combined snapshot, not the notification commit. Machine bindings now distinguish owned commit from observed_combined_head. All four file bytes/blob hashes are identical in both commits; actual pytest raw receipt never modified.

---

ci-tail-core-evidence.md

# CI tail core contracts before mutation

Base bdc5d6de3541d2deb95bf12198113ecea493c3a0. Scope only six indexed failures in packaging, skill linter/manager and shutdown fake. Exact pinned davey0.1.6/PyNaCl1.6.2 already approved: add only their exclude-newer-package exceptions in project and lock, preserve14-day global cutoff, all other exceptions/versions/artifacts/security scan. This is a genuine packaging metadata defect. Skill description budget remains1024; test advisory warning and edit/truncated preview using over-budget input, with exact-boundary no-warning pressure. Shutdown still clears served coverage and agent resources; fake supplies publication lifecycle, recording clear=True and retains cleanup assertions. No production Python changes indicated. Actual pytest baseline/final receipts remain separate. No slow release or previous26 audit.

## Skill manager actual contract correction

First focused attempt (ci-tail-core-final.json) exposed the independent1024-character authoring hard limit: simply making the old description1025 fails editing before preview. This is intentional validation, not a product defect. Retained it. Replaced obsolete old-budget edit+preview expectation with exact1024 full editable/persisted/no-truncation case, plus over-authoring-limit refusal preserving original bytes and a separate legacy external-content preview test with exact ellipsis/length/message. Original behavioral pressure is covered at its actual boundaries; no production validation changed. Original case maps to test_edit_long_desc_retains_full_approved_prompt_budget.

Focused intermediate correction ci-tail-core-corrected.json exposed two accidental extra positional arguments introduced by a broad import-text replacement in unrelated calls. Removed only those inserted arguments; retained raw red receipt and reran the whole skill manager file. No production defect inferred.

## Final core qualification

Commit 29ffab0af5ae088c0040ffaaa95b68c551b72012. Whole-file selected results: packaging10 (ci-tail-core-corrected.json), linter19/shutdown3/Discord packaging1/prompt3/served coverage10 (ci-tail-core-final.json), skill manager65 (ci-tail-skill-manager-final.json). Total111 passing cases across7 complete files;5subtests excluded from case total. Baseline ci-tail-core-before.json reproduced all6 indexed failures. No skip added. Offline actual uv lock --check returned0,255packages resolved; ci-tail-resolver-check.json records exact argv/environment/output. ci-tail-lock-integrity.json proves only two lock exceptions differ; package graph/artifacts/global cutoff unchanged. git diff --cached --check returned0 before commit.

## Preservation and handoff

Entire feature rows and historical checkpoint fields remain unchanged. The78 feature IDs have no pending implementation IDs; every manual check remains pending. The checklist is byte-identical to bdc5d6de35, preserving all five original retained manual checks and coordinated rollback boundaries. Prior source receipts and raw logs remain immutable. One targeted independent correction review is recorded in V021_APPROVED_CI_TAIL_REVIEW.md; external approved-ci-tail-evidence.md and approved-ci-tail-review.md bind the final documentation commit/tree and quiescence without source self-reference.

Independent targeted review accepted source29ffab0af5ae088c0040ffaaa95b68c551b72012/tree e407f1261ac2748e5a6fa44158a7d02a850d00b3 with no blocker. Independent108 cases across8 complete files passed, zero skips/failures; these overlap403 and are not added to total. Exact20 reviewed path hashes match final source.
